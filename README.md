# GurpsDomainMockServer 

This is a very simple node.js application that can serve as a mock 
for gurpsDomain. It will listen for a post on /convert and will respond 
with a json output sheet.

# Run
node index.js